export enum JOB_ROUTERS {
	ROUTER = "job",
	DEFAULT = "/",
	APPLY = "/apply",
	RECRUITER = "/recruiter",
	ALL = "/all",
	BLOCK = "/block/:jobId",
	APPLICATIONS = "/applications",
	ID_PARAM = "jobId",
}
